# Attendance Tracking System Flow

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        ATTENDANCE SYSTEM                         │
└─────────────────────────────────────────────────────────────────┘

┌──────────────┐         ┌──────────────┐         ┌──────────────┐
│   Frontend   │◄───────►│   Backend    │◄───────►│   MongoDB    │
│  (React.js)  │  WebRTC │  (Node.js)   │   ODM   │  (Database)  │
│              │  Socket │              │Mongoose │              │
└──────────────┘         └──────────────┘         └──────────────┘
       │                        │                        │
       │                        │                        │
   Face-API.js           Socket Events            Collections:
   Face Detection        Real-time Data          - Attendance
   Face Recognition      Event Handling          - Face
                                                 - Meeting
```

## User Flow - Step by Step

```
1. USER JOINS MEETING
   ┌─────────────┐
   │ Enter Name  │
   └──────┬──────┘
          │
          ▼
   ┌─────────────┐      ┌──────────────────┐
   │   Connect   │─────►│ First to join?   │
   └─────────────┘      └────┬─────────┬───┘
                             │ YES     │ NO
                             ▼         ▼
                      ┌──────────┐  ┌────────┐
                      │  OWNER   │  │ Member │
                      └──────────┘  └────────┘

2. FACE ENROLLMENT
   ┌──────────────────┐
   │  Enrollment      │
   │  Modal Opens     │
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐      ┌─────────────────┐
   │  Camera Starts   │─────►│  AI Models Load │
   └────────┬─────────┘      └─────────────────┘
            │
            ▼
   ┌──────────────────┐      ┌─────────────────┐
   │ Capture Face     │─────►│ Save Descriptor │
   └────────┬─────────┘      └─────────────────┘
            │
            ▼
   ┌──────────────────┐
   │ Tracking Starts  │
   └──────────────────┘

3. DURING MEETING (Every 10 seconds)
   ┌──────────────────┐
   │   Timer Tick     │
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐      ┌─────────────────┐
   │  Detect Face?    │─────►│  Face Found?    │
   └────────┬─────────┘      └────┬────────┬───┘
            │                     │ YES    │ NO
            │                     ▼        ▼
            │              ┌─────────┐ ┌────────┐
            │              │ +10 sec │ │ +0 sec │
            │              └────┬────┘ └───┬────┘
            │                   │          │
            │                   └──────┬───┘
            │                          │
            ▼                          ▼
   ┌──────────────────┐      ┌─────────────────┐
   │ totalTime += 10  │◄─────│ verifiedTime    │
   └────────┬─────────┘      └─────────────────┘
            │
            ▼
   ┌──────────────────┐
   │  Send to Server  │
   └──────────────────┘

4. END MEETING
   ┌──────────────────┐
   │  Owner Clicks    │
   │   "End Call"     │
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────────────────────────┐
   │  Calculate Attendance for Each User  │
   │                                      │
   │  percent = verifiedTime / totalTime  │
   │                                      │
   │  if (percent >= 75%) → Present       │
   │  else if (percent >= 50%) → Partial  │
   │  else → Absent                       │
   └────────┬─────────────────────────────┘
            │
            ▼
   ┌──────────────────┐      ┌─────────────────┐
   │  Generate Report │─────►│  Save to DB     │
   └────────┬─────────┘      └─────────────────┘
            │
            ├──────────┬──────────────┐
            ▼          ▼              ▼
   ┌────────────┐ ┌────────────┐ ┌──────────────┐
   │ Send to    │ │ Send to    │ │ Special      │
   │ All Users  │ │ Database   │ │ Owner Report │
   └────────────┘ └────────────┘ └──────────────┘
```

## Data Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                    DATA TRACKING FLOW                        │
└─────────────────────────────────────────────────────────────┘

User Joins
    ↓
┌──────────────────┐
│ userId: "john"   │
│ totalTime: 0     │─────┐
│ verifiedTime: 0  │     │  Stored in Memory
└──────────────────┘     │  (connections object)
                         │
Every 10 seconds         │
    ↓                    │
┌──────────────────┐     │
│ Face Detected?   │     │
└────┬──────┬──────┘     │
     │ YES  │ NO         │
     ▼      ▼            │
  +10s    +0s           │
     │      │            │
     └──┬───┘            │
        ↓                │
┌──────────────────┐     │
│ totalTime += 10  │◄────┘
│ verifiedTime +=  │
│   (10 or 0)      │
└──────────────────┘
        ↓
    Continues...
        ↓
Meeting Ends
        ↓
┌──────────────────────────┐
│ Final Calculation:       │
│                          │
│ totalTime: 600s (10 min) │
│ verifiedTime: 480s (8m)  │
│                          │
│ percent = 480/600 = 80%  │
│ status = "Present"       │
└────────┬─────────────────┘
         ↓
┌──────────────────┐
│  Save to MongoDB │
└──────────────────┘
```

## Attendance Calculation Logic

```
┌─────────────────────────────────────────────────────┐
│              ATTENDANCE STATUS LOGIC                 │
└─────────────────────────────────────────────────────┘

User's verifiedPercent
         │
         ▼
    ┌────────┐
    │ ≥ 75%? │───YES──► Present ✅
    └───┬────┘
        │ NO
        ▼
    ┌────────┐
    │ ≥ 50%? │───YES──► Partial ⚠️
    └───┬────┘
        │ NO
        ▼
      Absent ❌

Examples:
┌──────────────┬──────────┬───────────┬────────┐
│ Total Time   │ Verified │ Percent   │ Status │
├──────────────┼──────────┼───────────┼────────┤
│ 600s (10min) │ 500s     │ 83%       │ ✅ P   │
│ 600s (10min) │ 400s     │ 67%       │ ⚠️ PA  │
│ 600s (10min) │ 250s     │ 42%       │ ❌ A   │
│ 300s (5min)  │ 240s     │ 80%       │ ✅ P   │
│ 300s (5min)  │ 180s     │ 60%       │ ⚠️ PA  │
│ 300s (5min)  │ 120s     │ 40%       │ ❌ A   │
└──────────────┴──────────┴───────────┴────────┘
```

## Socket Events Flow

```
┌─────────────────────────────────────────────────────┐
│              SOCKET.IO EVENT SEQUENCE                │
└─────────────────────────────────────────────────────┘

Client (Frontend)          Server (Backend)
      │                          │
      │───── join-call ─────────►│
      │   (path, userId,         │ • Create/join room
      │    isOwner)              │ • Track as owner if first
      │                          │ • Store user data
      │◄──── user-joined ────────│
      │                          │
      │                          │
      │─── register-face ───────►│
      │   (meetingId,            │ • Save face descriptor
      │    userId,               │   to database
      │    descriptor)           │ • Link to userId
      │◄─ face-registered ───────│
      │                          │
      │                          │
      │ (Every 10 seconds)       │
      │─ verified-update ───────►│
      │   (meetingId,            │ • Update totalTime
      │    userId,               │ • Update verifiedTime
      │    verifiedDelta)        │ • Store in memory
      │                          │
      │        ...               │
      │        ...               │
      │                          │
      │                          │
      │──── end-meeting ────────►│
      │   (meetingId)            │ • Calculate attendance
      │                          │ • Generate report
      │                          │ • Save to database
      │                          │
      │◄─ attendance-report ─────│ (to all users)
      │                          │
      │◄ owner-attendance- ──────│ (to owner only)
      │   report                 │
      │                          │
```

## Database Schema

```
┌─────────────────────────────────────────────────────┐
│                  MONGODB COLLECTIONS                 │
└─────────────────────────────────────────────────────┘

Collection: Attendance
┌──────────────────────────────────────────────┐
│ {                                            │
│   _id: ObjectId,                             │
│   meetingId: "http://localhost:3000/meet1",  │
│   meetingOwner: "john_doe",                  │
│   startTime: 2026-01-08T10:00:00Z,          │
│   endTime: 2026-01-08T10:10:00Z,            │
│   participants: [                            │
│     {                                        │
│       userId: "student1",                    │
│       name: "student1",                      │
│       totalTime: 600,                        │
│       verifiedTime: 480,                     │
│       verifiedPercent: 80,                   │
│       status: "Present"                      │
│     },                                       │
│     {                                        │
│       userId: "student2",                    │
│       totalTime: 600,                        │
│       verifiedTime: 360,                     │
│       verifiedPercent: 60,                   │
│       status: "Partial"                      │
│     }                                        │
│   ],                                         │
│   date: 2026-01-08T10:10:00Z                │
│ }                                            │
└──────────────────────────────────────────────┘

Collection: Face (Temporary)
┌──────────────────────────────────────────────┐
│ {                                            │
│   _id: ObjectId,                             │
│   userId: "student1",                        │
│   meetingId: "http://localhost:3000/meet1",  │
│   descriptor: [128 numbers...]               │
│ }                                            │
│ ⚠️ Deleted after meeting ends                │
└──────────────────────────────────────────────┘

Collection: Meeting
┌──────────────────────────────────────────────┐
│ {                                            │
│   _id: ObjectId,                             │
│   user_id: "student1",                       │
│   meetingCode: "meet1",                      │
│   meetingOwner: "john_doe",  ← NEW           │
│   date: 2026-01-08T10:00:00Z                │
│ }                                            │
└──────────────────────────────────────────────┘
```

## API Endpoints

```
┌─────────────────────────────────────────────────────┐
│                  REST API ROUTES                     │
└─────────────────────────────────────────────────────┘

GET /api/v1/attendance/owner/:userId
    ↓
┌─────────────────────────────────────┐
│ Returns all meetings where user     │
│ is the owner                        │
│                                     │
│ Response: Array of attendance       │
│ reports with full details           │
└─────────────────────────────────────┘

GET /api/v1/attendance/meeting/:meetingId
    ↓
┌─────────────────────────────────────┐
│ Returns specific meeting's          │
│ attendance report                   │
│                                     │
│ Response: Single attendance report  │
└─────────────────────────────────────┘

GET /api/v1/attendance/user/:userId
    ↓
┌─────────────────────────────────────┐
│ Returns all meetings where user     │
│ was a participant                   │
│                                     │
│ Response: Array with user's         │
│ attendance in each meeting          │
└─────────────────────────────────────┘
```

## Component Hierarchy

```
┌─────────────────────────────────────────────────────┐
│                    REACT COMPONENTS                  │
└─────────────────────────────────────────────────────┘

App.js
 │
 ├─ VideoMeet.jsx (Main Meeting Component)
 │   │
 │   ├─ Lobby View
 │   │   └─ Username Input
 │   │   └─ Preview Video
 │   │
 │   ├─ Meeting View
 │   │   ├─ Local Video (with face detection)
 │   │   ├─ Remote Videos
 │   │   ├─ Control Buttons
 │   │   │   ├─ Video Toggle
 │   │   │   ├─ Audio Toggle
 │   │   │   ├─ Screen Share
 │   │   │   ├─ End Call ← Triggers report
 │   │   │   └─ Chat
 │   │   │
 │   │   ├─ Enrollment Modal
 │   │   │   ├─ Camera Preview
 │   │   │   └─ Capture Button
 │   │   │
 │   │   └─ Attendance Report Modal
 │   │       ├─ Owner Badge (if owner)
 │   │       ├─ Criteria Explanation
 │   │       └─ Participant Table
 │   │
 │   └─ Face Detection System
 │       ├─ Face-API Models
 │       ├─ 10s Interval Timer
 │       └─ Socket Updates
 │
 └─ AttendanceHistory.jsx (Optional)
     ├─ Report Cards (Expandable)
     │   ├─ Meeting Info
     │   ├─ Summary Stats
     │   └─ Detailed Table
     └─ Filter/Sort Options
```

---

Legend:
  ✅ = Present (≥75%)
  ⚠️ = Partial (50-74%)
  ❌ = Absent (<50%)
  👑 = Meeting Owner
  P = Present
  PA = Partial
  A = Absent
